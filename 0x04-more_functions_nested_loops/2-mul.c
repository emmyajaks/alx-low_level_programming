#include "main.h"
#include <stdio.h>
/**
 * mul - it multiplies two intergers
 * @a: the first interger
 * @b: the second interger
 * Return: Always 0
 */

int mul(int a, int b)
{
	int result = a * b;
return (result);
}
